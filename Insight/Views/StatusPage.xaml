<Page
    x:Class="Insight.Views.StatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:tg="using:Telerik.UI.Xaml.Controls.Grid"
    xmlns:model="using:Insight.Core.Models"
    mc:Ignorable="d">
    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
		<Grid Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
			<!--TODO : These column headers look ugly when they are AutoGenerated. StringsLookLikeThis-->
			<tg:RadDataGrid ColumnDataOperationsMode="Flyout" x:Name="grid" ItemsSource="{x:Bind ViewModel.Source, Mode=OneWay}" AutoGenerateColumns="True" UserEditMode="External" CanDrag="True" CanUserChooseColumns="True">
                <tg:RadDataGrid.Columns>
					<!--Keep these column definitions as examples of how to make defined columns for certain pages-->
                    <!--TODO WTS:
                      Remove this column definitions and define columns for your data.
                      Consider adding header properties to Resources.resw-->
                    <!--<tg:DataGridTextColumn Header="Last, First" PropertyName="Name" />
                    <tg:DataGridTextColumn Header="Flight" PropertyName="Flight" />
                    <tg:DataGridTextColumn Header="SSN"  PropertyName="SSN" />
                    <tg:DataGridTextColumn Header="Medical" PropertyName="Medical.OverallStatus" />
                    <tg:DataGridTextColumn Header="Training" PropertyName="Training.OverallStatus" />
                    <tg:DataGridTextColumn Header="Personnel"  PropertyName="Personnel.OverallStatus" />
                    <tg:DataGridTextColumn Header="Phone"  PropertyName="Phone" />-->
                    <!--<tg:DataGridTemplateColumn Header="Symbol">
                        <tg:DataGridTemplateColumn.CellContentTemplate>
                            <DataTemplate x:DataType="model:SampleOrder">
                                <FontIcon
                                      HorizontalAlignment="Left"
                                      Margin="{StaticResource MediumLeftRightMargin}"
                                      FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                      Glyph="{x:Bind Symbol}" />
                            </DataTemplate>
                        </tg:DataGridTemplateColumn.CellContentTemplate>
                    </tg:DataGridTemplateColumn>-->
                </tg:RadDataGrid.Columns>
            </tg:RadDataGrid>
        </Grid>
    </Grid>
</Page>
